1. git init, npm init -y, README.md, .gitignore, .gitattributes,index.html, index.js
2. добавление команды запуска dev-сервера без установки зависимостей в проект (npm run dev)
3. `npm install lodash`
4. `npm install jest @types/jest -D` => `npx create-jest`
5. `npm install jest-environment-jsdom -D`
6. `npm install --save-dev babel-jest @babel/core @babel/preset-env` => создать babel.config.js
7. `npm install --save-dev eslint` => `npx eslint --init`
8. `npm install eslint-plugin-jest -D` => поправить eslint.config.mjs
9. `npm install --save-dev --save-exact prettier` => создать .prettierrc
10. `npm install --save-dev eslint-plugin-prettier eslint-config-prettier` => поправить eslint.config.mjs
11. `npm install --save-dev husky lint-staged` => `npx husky init` => заменить содержимое файла pre-commit => добавить секцию в package.json
12. `npm install --save-dev webpack webpack-cli webpack-dev-server html-webpack-plugin css-loader style-loader babel-loader` => создать webpack.config.js
13.
